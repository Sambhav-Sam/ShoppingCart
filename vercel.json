{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "zeroConfig": true
            }
        }
    ],
    "build": {
        "env": {
            "NPM_CONFIG_LEGACY_PEER_DEPS": "true"
        }
    }
}